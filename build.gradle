version = '0.9.0'
group = 'org.juankysoriano'

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.0.0-rc4'
    }
}

repositories {
    mavenCentral()
}

ext {
    COMPILE_SDK = 21
    TARGET_SDK = 17
    MIN_SDK = 16
    BUILD_TOOLS = '20.0.0'
    VERSION_CODE = 1914091
    VERSION_NAME = "0.9.5"
}
